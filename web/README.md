Run

1) Install dependency (if needed):
   pip install streamlit

2) Start:
   streamlit run web/stock_backtest_app.py

Data source: `stock_results_rolling`

Daily predictions UI:

1) Install dependency (if needed):
   pip install streamlit

2) Start:
   streamlit run web/stock_daily_app.py

Data source: `stock_results_daily`
